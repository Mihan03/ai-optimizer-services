# Имя, чтобы представиться Config Server'у и Eureka
spring:
  application:
    name: eureka-server
  
  # 👇 ВОТ ЭТО ИСПРАВЛЕНИЕ 👇
  # Указываем, откуда импортировать внешние настройки.
  # Это нужно сделать до того, как будут применены остальные конфиги.
  config:
    import: "optional:configserver:http://localhost:8888"